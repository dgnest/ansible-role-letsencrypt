---
# vars file for dgnest.letsencrypt

# letsencrypt_create_files:
#   - path: "{{ _path_dir }}"
#     state: "directory"
#     # group: "root"
#     owner: "{{ user }}"


# letsencrypt_templates:
#   - src: "default.j2"
#     dest: {{ letsencrypt_file }}

_letsencrypt_pip_packages:
  - blist
  - ansicolors
  - letsencrypt

_letsencrypt_packages:
  - python
  - python-dev
  - python-virtualenv
  - gcc
  - dialog
  - libaugeas0
  - libssl-dev
  - libffi-dev
  - ca-certificates
  - python-pip
  - git
