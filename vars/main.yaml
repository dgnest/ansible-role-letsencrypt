---
# vars file for dgnest.letsencrypt

# _dgnest.letsencrypt_create_files:
#   - path: "{{ _path_dir }}"
#     state: "directory"
#     # group: "root"
#     owner: "{{ user }}"


# _dgnest.letsencrypt:
#   templates:
#     - src: "default.j2"
#       dest: {{ _dgnest.letsencrypt_file }}

_letsencrypt_pip_packages:
  - setuptools
  - pip
  - letsencrypt

_letsencrypt_packages:
  - python
  - python-dev
  - python-virtualenv
  - gcc
  - dialog
  - libaugeas0
  - libssl-dev
  - libffi-dev
  - ca-certificates
  - python-pip
  - git
  - virtualenv
